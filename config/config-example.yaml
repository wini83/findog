# Example mapping for an API consumer. The Python adapter receives this mapping
# directly; this file is deliberately not loaded by the library.
excel_dropbox_path: "/Oplaty.xlsm"

monitored_sheets:
  Home: ["C", "I", "O"]
